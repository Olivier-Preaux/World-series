{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ season.description }}</td>
            </tr>
        </tbody>
    </table>
<h2>Les épisodes de la saison : </h2></br>
    {% for episode in episodes %}
    <a href="{{ path('program_show_episode', { 'program_id' : program.id , 'season_id': season.id , 'episode_id': episode.id }) }}">Episode {{ episode.number }} </a>: {{ episode.title }}</br>
    {% endfor %}
    </br>

    <a href="{{ path('program_index') }}">back to list</a>

    <a href="{{ path('season_edit', {'id': season.id}) }}">edit</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
